// Smoke test placeholder. Real integration tests live under
// /app/mobile/integration_test (to be added).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke', () {
    expect(1 + 1, 2);
  });
}
